const arr = ['Face ID', 'accelerometer', 'gyro', 'proximity', 'compass', 'barometer']
for (let i = 0; i < arr.length; i++) {
    const element = arr[i];
    // console.log(element);
    
}
arr.forEach(element => {
    console.log(element);
});